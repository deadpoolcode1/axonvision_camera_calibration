# AxonVision Camera Calibration - Production Configuration
# Overrides default.yaml for production environment

logging:
  level: "INFO"
  file:
    enabled: true
    path: "/var/log/axonvision"
    filename: "axonvision.log"
    max_size_mb: 50
    backup_count: 10
